'use strict';

for(let i=0; i<=5; i++){
    const input= Number(prompt('Inserisci un numero'));
    console.log(input);
};

let inputNum = 0;

while(inputNum < 5){
    inputNum++;
    const input = Number(prompt('Inserisci un numero')); 
    console.log(input);
    inputNum<5;
}